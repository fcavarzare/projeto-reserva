apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-ui
  namespace: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: web-ui
  template:
    metadata:
      labels:
        app: web-ui
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - web-ui
              topologyKey: "kubernetes.io/hostname"
      containers:
      - name: web-ui
        image: fcavarzare/projeto-reserva:web-ui-6d7c0da1d48f8741b6ffd7e835a6aca55d72c1e2
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "100m"
            memory: "512Mi"
          requests:
            cpu: "100m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: web-ui-service
  namespace: dev
spec:
  selector:
    app: web-ui
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: projeto-reserva-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: projeto-reserva.dev.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-ui-service
            port:
              number: 80
      - path: /api/reservations
        pathType: Prefix
        backend:
          service:
            name: booking-api-service
            port:
              number: 80
      - path: /api/auth
        pathType: Prefix
        backend:
          service:
            name: identity-api-service
            port:
              number: 80
      - path: /api/payments
        pathType: Prefix
        backend:
          service:
            name: payment-api-service
            port:
              number: 80
