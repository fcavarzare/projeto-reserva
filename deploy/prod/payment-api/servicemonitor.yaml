apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: payment-api
  namespace: prod
  labels:
    release: kps
    app: payment-api
spec:
  selector:
    matchLabels:
      app: payment-api
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
