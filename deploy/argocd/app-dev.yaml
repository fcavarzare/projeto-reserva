apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: projeto-reserva-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/fcavarzare/projeto-reserva.git
    targetRevision: main
    path: deploy/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
